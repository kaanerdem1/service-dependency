# Radial / harita notları

## Uygulandı

1. **Ok akış animasyonu** — hover/focus yolunda `dd-edge-flow` (`App.css`, `dd-edge-on`)
2. **Katman stagger reveal** — hop expand’de iç `dd-node` motion girişi (`revealIndex`, `listItemTransition`)
3. **Pivot halkası shared-layout** — merkez `layoutId="impact-pivot-ring"` (`ImpactMap.tsx`)
4. **Method skeleton** — `MethodLeaves` → `SkeletonShimmer` (`ModuleTree.tsx`)
5. **Tema ikonu crossfade** — `ThemeSwitch.tsx`
6. **Etki şeridi segmented bar** — `BlastRadiusSummary` (`ImpactChrome.tsx`)
