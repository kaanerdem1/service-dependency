/**
 * Servis etki haritası (React Flow, gelişmiş görünüm).
 *
 * Uygulama: `impactMap/ImpactMapCore.tsx` + parçalar (nodes, edges, buildGraph, popovers).
 * CSS: `styles/service-map.css`. Kullanan: `MapStage.tsx` → `ServiceStage.tsx`.
 */
export { ImpactMap } from './impactMap/ImpactMapCore'
export type { ImpactMapProps } from './impactMap/types'
